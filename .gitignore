<<<<<<< HEAD
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]

# C extensions
*.so

# Distribution / packaging
.Python
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
#  Usually these files are written by a python script from a template 
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.cache
nosetests.xml
coverage.xml

# Translations
*.mo
*.pot

# Django stuff:
*.log

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# Video-Output
*.ts
*.mkv
*.mp4

#editor backups
*~
*.swp

# override-configs
/voctocore/config.ini
/voctogui/config.ini
/example-scripts/config.sh
/example-scripts/voctomidi/config.ini

# IDE project files
/*.iml
/.idea

# --- LaTeX compiled output (raíz y memoria_tfg) ---
/*.aux
/*.bbl
/*.blg
/*.fdb_latexmk
/*.fls
/*.lof
/*.log
/*.lot
/*.out
/*.synctex.gz
/*.synctex(busy)
/*.toc
/*.toc
# main.pdf en raíz se conserva accesible (no ignorado)
memoria_tfg/*.aux
memoria_tfg/*.bbl
memoria_tfg/*.blg
memoria_tfg/*.log
memoria_tfg/*.pdf
memoria_tfg/*.toc
memoria_tfg/*.lof
memoria_tfg/*.lot
memoria_tfg/*.out
memoria_tfg/*.fls
memoria_tfg/*.fdb_latexmk
memoria_tfg/pdflatex*.fls

# --- Runtime telemetry output ---
registros/

# --- Video assets (too large for git, add manually to releases) ---
videos/

# --- Backup files ---
*.bak
*.bak2
*.bak3
*\ (copia).*
*\ (otra\ copia).*

# --- Project-specific temp files ---
historial_gemini.txt
notion_tfg/

# --- Sessions ---
sessions/

# --- Kubernetes secrets (never commit real credentials) ---
k8s/secret.yaml

# --- Claude Code internal files ---
# .claude/ is intentionally tracked (project rules, docs, templates)
# Only exclude user-specific local settings:
.claude/settings.local.json

# --- Environment variables (may contain credentials) ---
.env

# --- Paper (Overleaf git repo — managed separately, not nested in voctomix git) ---
paper/
=======
__pycache__/
*.pyc
registros/
sessions/
*.log
.env
>>>>>>> 920adf625f85240b9410c616f7c7c875f625df30
