\section{Fuentes de señal de cámaras}
\label{sec:fuentes_camaras}

El sistema acepta hasta cuatro fuentes de cámara simultáneas (cam1--cam4), que
constituyen las entradas principales del mezclador. Cada cámara envía su señal al
núcleo mediante una conexión TCP en formato Matroska con vídeo en bruto (I420) y audio
PCM S16LE a 48~kHz estéreo.

\subsection{Conexión de las fuentes}

Cada fuente de cámara se conecta al puerto TCP correspondiente de \texttt{voctocore}:

\begin{itemize}
  \item \textbf{cam1} $\rightarrow$ puerto 10000
  \item \textbf{cam2} $\rightarrow$ puerto 10001
  \item \textbf{cam3} $\rightarrow$ puerto 10002
  \item \textbf{cam4} $\rightarrow$ puerto 10003
\end{itemize}

El envío de la señal se realiza mediante FFmpeg~\cite{ffmpeg_project}, herramienta de
código abierto para el procesamiento multimedia, que encapsula la señal en un flujo
Matroska y lo transmite por TCP hacia el puerto correspondiente de \texttt{voctocore}. En el
entorno de desarrollo y pruebas, FFmpeg inyecta vídeos pregrabados de un evento real,
grabaciones de distintas cámaras colocadas en diferentes puntos de un auditorio durante
unas conferencias técnicas, que se reproducen en bucle para emular la presencia de
cámaras en directo. En un despliegue con cámaras físicas, el mismo proceso FFmpeg se
ejecuta desde el equipo servidor conectado a la tarjeta de captura.

\subsection{Previsualización en la interfaz gráfica}

Una vez conectadas, las fuentes de cámara aparecen automáticamente en el panel de
previsualizaciones de \texttt{voctogui}. La interfaz muestra cuatro miniaturas JPEG de las
cuatro cámaras, recibidas desde los puertos 14000--14003 a una resolución reducida
(1024$\times$576) para minimizar el consumo de ancho de banda entre el servidor y el cliente
de control. Cada miniatura incluye:

\begin{itemize}
  \item Un indicador visual del nombre de la fuente (cam1, cam2, cam3, cam4).
  \item Botones de selección para asignar la fuente al bus A o al bus B del mezclador.
  \item Un indicador del nivel de audio asociado a esa fuente.
\end{itemize}

El realizador selecciona la fuente activa haciendo clic sobre la miniatura
correspondiente, lo que envía el comando \texttt{set\_video\_a <fuente>} o
\texttt{set\_video\_b <fuente>} al núcleo mediante el protocolo de control TCP.

\begin{figure}[H]
  \centering
  \savebox{\panelcambox}{\includegraphics[width=0.33\textwidth]{figuras/panel_camaras1.png}}%
  \begin{minipage}[t]{0.33\textwidth}
    \vspace{0pt}
    \subcaptionbox{Previsualizaciones y niveles de audio.\label{fig:panel_camaras_previews}}{%
      \usebox{\panelcambox}}
  \end{minipage}%
  \hfill
  \begin{minipage}[t][\ht\panelcambox][s]{0.62\textwidth}
    \vspace{1.0cm}
    \subcaptionbox{Detalle de CAM1 con su medidor.\label{fig:panel_camaras_detalle}}{%
      \includegraphics[width=0.8\textwidth]{figuras/panel_camaras2.png}}
    \vfill
    \subcaptionbox{Selectores de canal A y canal B.\label{fig:panel_camaras_selectores}}{%
      \includegraphics[width=0.8\textwidth]{figuras/panel_camaras3.png}}
    \vspace{1.0cm}
  \end{minipage}
  \caption{Panel de cámaras en \texttt{voctogui}.}
  \label{fig:panel_camaras}
\end{figure}

\subsection{Requisitos de formato de la señal de entrada}
\label{sec:formato_entrada}

Durante el desarrollo se comprobó que \texttt{voctocore} es estricto con los metadatos del flujo
de vídeo entrante. Al intentar inyectar un archivo \texttt{.mp4} convencional como
fuente de prueba, el núcleo rechazaba la conexión sin emitir un mensaje de error claro.
Tras el diagnóstico se identificaron un requisito obligatorio y una condición de
calidad que toda fuente debe satisfacer:

\begin{enumerate}
  \item \textbf{Pista de audio obligatoria:} \texttt{voctocore} exige que cada flujo Matroska
        contenga tanto vídeo como audio. Los vídeos sin audio deben complementarse con
        una pista de silencio generada en tiempo real por FFmpeg mediante el filtro
        \texttt{-f lavfi -i aevalsrc=0}. Sin este ajuste, el motor GStreamer cierra la
        conexión en el \textit{handshake} inicial.
  \item \textbf{Colorimetría televisiva (BT.709)}~\cite{itu_bt709}: se recomienda que los metadatos del
        flujo declaren el espacio de color estándar de alta definición
        (\texttt{colorimetry=bt709}) y el rango de luz televisivo (\texttt{tv range}).
        Los archivos grabados con cámaras convencionales o editados con software de
        consumo suelen emplear el rango \texttt{pc} (full range). Sin la conversión
        adecuada, los colores presentan una desviación perceptible en la mezcla, aunque
        la conexión se establece correctamente. Para convertirlos, se aplican los
        filtros FFmpeg \texttt{scale=out\_range=tv} y \texttt{colorspace=bt709}, que
        recalculan los valores de píxel en tiempo real sin degradación visual perceptible.
\end{enumerate}

En definitiva, todos los parámetros del flujo entrante, resolución, frecuencia de imagen,
espacio de color y formato de audio, deben coincidir con los valores declarados en el
fichero de configuración INI de \texttt{voctocore} (\texttt{default-config.ini}), que fija los
parámetros globales del sistema: 1920$\times$1080, I420, 25~fps y 48~kHz S16LE estéreo.
Es el propio \textit{pipeline} GStreamer el que impone esta restricción: cualquier discrepancia
entre la fuente y la configuración provoca un rechazo de conexión o artefactos en la
mezcla, como se comprobó durante el desarrollo.
