\section{Sistema de telemetría}

El sistema de telemetría de Voctomix 2.0 permite registrar en tiempo real todos los
eventos que ocurren durante una sesión de producción: cambios de fuente activa, cambios
de composite, activaciones del stream blanker, carga y descarga de overlays, etc. Esta
información resulta valiosa tanto para la monitorización durante el directo como para
el análisis posterior de la sesión.

\subsection{Servicio de telemetría}

El servicio de telemetría (\texttt{example-scripts/ffmpeg/telemetry\_service.py}) es un
proceso Python independiente que se suscribe al protocolo de control TCP de voctocore
(puerto 9999) como un cliente más. Registra todos los cambios de estado que el núcleo
difunde por broadcast y los exporta en dos canales complementarios:

\begin{itemize}
  \item \textbf{API REST HTTP (puerto 8080):} devuelve el estado actual completo de la
        sesión en formato JSON, accesible por cualquier cliente HTTP. Resulta útil para
        dashboards de monitorización en tiempo real o para scripts de automatización que
        necesiten consultar el estado del sistema.
  \item \textbf{Mensajería AMQP (RabbitMQ):} cada cambio de estado se publica como un
        mensaje en el broker RabbitMQ, permitiendo su consumo por sistemas externos de
        registro, alertas o automatización mediante colas de mensajes.
\end{itemize}

La arquitectura desacoplada del servicio de telemetría ---conectado como un cliente TCP
ordinario, sin privilegios especiales sobre el núcleo--- garantiza que su eventual fallo
no afecte al procesamiento multimedia principal. El sistema de producción puede operar
con plenas garantías aunque el servicio de telemetría no esté disponible
(\textit{single point of failure} libre).

\subsection{RabbitMQ como broker de mensajería}

RabbitMQ es el broker de mensajería AMQP que centraliza los eventos de telemetría. En
el despliegue Docker, se ejecuta como un contenedor independiente accesible en el puerto
5672 (protocolo AMQP) y 15672 (consola web de administración). Los credenciales por
defecto son \texttt{voctomix}/\texttt{voctomix123}.

Cada evento de sesión (cambio de fuente, cambio de composite, activación de stream
blanker, etc.) se publica como un mensaje JSON en una cola de RabbitMQ. Esto permite:

\begin{itemize}
  \item Mantener un registro completo y persistente de todos los eventos de la sesión,
        con marca de tiempo, para análisis posterior.
  \item Integrar Voctomix con sistemas externos de monitorización, logging centralizado
        (ELK Stack, Grafana) o automatización mediante consumidores de la cola.
  \item Verificar el correcto funcionamiento del sistema durante las pruebas, comprobando
        que cada acción del realizador genera el evento de telemetría correspondiente.
\end{itemize}

Un ejemplo de consumidor de la cola RabbitMQ se encuentra en
\texttt{rabbit\_consumer\_example.py}, que sirve como punto de partida para integrar
Voctomix con sistemas externos.

% --- Figura sugerida ---
% \begin{figure}[H]
%   \centering
%   \includegraphics[width=0.85\textwidth]{figuras/integracion_telemetria.png}
%   \caption{Diagrama de integración: voctocore -- telemetría -- RabbitMQ -- consumidores.}
%   \label{fig:integracion_telemetria}
% \end{figure}
